package sigmacine.ui.controller;

/**
 * Thin subclass for naming clarity: the previous "ContenidoCarteleraController"
 * is used as the implementation. This class simply extends it so the FXML
 * can refer to a clearer name `VerDetallePeliculaController`.
 */
public class VerDetallePeliculaController extends ContenidoCarteleraController {
    // no changes — this is purely a renaming wrapper to allow separate FXML
}
